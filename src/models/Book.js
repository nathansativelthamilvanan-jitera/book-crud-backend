class Book {
  constructor(id, title, publication_year, author_id) {
      this.id = id;
      this.title = title;
      this.publication_year = publication_year;
      this.author_id = author_id;
  }
}

module.exports = Book;
